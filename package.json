{
  "name": "autocomplete",
  "scripts": {
    "build": "lerna bootstrap && lerna run build",
    "release": "npm run build && lerna publish --no-git-reset"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
